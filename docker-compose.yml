version: "3.9"

services:

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus-buntodo
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    networks:
      - default

  grafana:
    image: grafana/grafana:latest
    container_name: grafana-buntodo
    ports:
      - "3001:3000" # Access at localhost:3001
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
    networks:
      - default

  # ---------- Postgres ----------
  postgres-buntodo:
    image: postgres:14
    container_name: postgres-buntodo
    environment:
      POSTGRES_USER: bunuser
      POSTGRES_PASSWORD: bunpass
      POSTGRES_DB: buntododb
    ports:
      - "5432:5432"
    volumes:
      - buntodo_pgdata:/var/lib/postgresql/data

  # ---------- Redis ----------
  redis-buntodo:
    image: redis:7
    container_name: redis-buntodo
    ports:
      - "6379:6379"
    volumes:
      - buntodo_redisdata:/data

  # ---------- Bun backend ----------
  backend-buntodo:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app       # live reload
    environment:
      - DATABASE_URL=postgres://bunuser:bunpass@postgres-buntodo:5432/buntododb
      - REDIS_URL=redis://redis-buntodo:6379
      - PORT=3000
    expose:
      - "3000"         # internal container port
    deploy:
      replicas: 4      # 4 backend containers
    depends_on:
      - postgres-buntodo
      - redis-buntodo

  # ---------- Nginx ----------
  nginx-buntodo:
    image: nginx:alpine
    container_name: nginx-buntodo
    ports:
      - "8080:80"     # host port → container port
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend-buntodo

volumes:
  buntodo_pgdata:
  buntodo_redisdata:
  prometheus_data-buntodo:   # Persistence for metrics
  grafana_data-buntodo:      # Persistence for Grafana dashboards